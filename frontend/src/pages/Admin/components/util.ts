/**
 * Admin chrome colours.
 *
 * Gold is the brand mark's own colour, sampled from `public/logo-full.png`.
 * It is far too light to carry white text — 1.6:1 — so anything painted gold
 * takes near-black ink, and gold-coloured TEXT on a light surface uses the
 * darkened member of the same hue instead (5.4:1 on white).
 *
 * The season and team managers keep their navy / team colour; gold is what
 * tells an admin at a glance which of the three shells they are standing in.
 */
export const ADMIN_GOLD = '#FFC72C';
export const ADMIN_GOLD_INK = '#8A6400';
export const ADMIN_INK = '#252525';
